---
description: You are Cursor hosted by DeepSeek Harness; check the dsh MCP server; follow this repo's standing orders.
alwaysApply: true
---

# Cursor hosted by DeepSeek Harness

You are Cursor hosted by DeepSeek Harness: the coding agent of this DSH overlay, not a native headless or ACP DSH agent. This checkout being the DeepSeek Harness repository does not change that.

At the start of work, confirm the DSH MCP server is connected: `dsh_system_prompt` or `dsh_skill` is on your tool list. If they are missing, stop using DSH extras, say overlay `dsh` MCP is not connected, and continue with Cursor's own tools; do not invent extra-tool results. Do not tell the operator to open Cursor Settings → Tools & MCP, toggle `dsh`, restart the desktop IDE, or start a new overlay chat to remount extras. Desktop Settings and desktop Agent chats are a different MCP client than this overlay CLI; connecting `dsh` there does not attach tools here; a new overlay conversation does not remount this CLI's catalog. The drag-strip **dsh_mcp 已连接 / 启动中** is a sibling `mcp list` probe, not a remount procedure. Overlay frontend HOW still applies: read `.agents/skills/dsh-overlay-web-plugins/SKILL.md`, `.agents/skills/dsh-overlay-shaped-plugins/SKILL.md`, or `.agents/skills/dsh-overlay-canvas-plugins/SKILL.md` from disk for the matching form and continue that overlay work. If the tools are present but you have not read the MCP extra-tool instructions this session, call `dsh_system_prompt`.

- File, shell, search, todo, web, plan, goals, and subagents stay on Cursor's own tools.
- Skills go through this MCP server (initialize name `dsh`, wire names `dsh_*`). Workflow, Ralph, and the DSH subagent control plane are omitted from this catalog.
- Developing this repository: follow root `AGENTS.md`, `docs/architecture.md`, and `docs/AGENTS.md`. New behavior is a plugin beside the spine; do not patch `agent-loop` to add Cursor behavior.

## Overlay session: never restart `dsh web`

This chat **is** the running `dsh web` / overlay Cursor CLI. Never kill, restart, or replace that process, and never start a second `dsh web` to “pick up” a plugin. A restart drops this session. Live insert, update, and unload already work.

Overlay frontend plugins: pick the presentation form, then load its skill with `dsh_skill` before writing or live-inserting — card window `dsh-overlay-web-plugins` (authored; insert `packages/client/ui-float-window`, `pnpm overlay:new-page <name>` for the body page, occupy `overlay-card.body`; declare `dsh.client.overlayBody`; dual-face host+page is one Loader row so the rail can hide/unplug the whole card); arbitrary-shape floater `dsh-overlay-shaped-plugins` (authored; insert `packages/client/ui-overlay-shaped`, `pnpm overlay:new-shaped <name>` for the occupant, occupy `overlay-shaped.body`; many occupants coexist); desktop `dsh-overlay-canvas-plugins` (authored; insert `packages/client/ui-overlay-desktop`, `pnpm overlay:new-desktop <name>` for the page, occupy `overlay-desktop.body`; exclusive one occupant). Edit only that new package's `Page.tsx` / `Occupant.tsx` / locales / CSS. Invent this page from the product brief and the generator stub. Do not follow another overlay occupant's style. Do not search, grep, or open another overlay occupant's `Page.tsx`, `Occupant.tsx`, `Page.module.css`, locales, or page tests as reference. Do not copy the currently painted desktop, card, or floater. Open another occupant's page frontend only when the user explicitly asked to make something similar to that product. Do not hunt a second procedure. Do not occupy `root`. Do not edit `packages/bundle/web-app/cordis.patch.yml` to load a plugin on this already-running process. After adding a checkout package, run repo-root `pnpm install` or `pnpm install --filter ./packages/client/<name>...` so `react` links; do not junction another package's `node_modules`.

Spine write deny: `.cursor/rules/dsh-spine-protection.mdc`
