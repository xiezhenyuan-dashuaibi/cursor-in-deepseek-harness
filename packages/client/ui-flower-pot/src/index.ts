/**
 * @deepseek-ai/dsh-client-ui-flower-pot node half. The browser half ships via
 * exports["./client"] and occupies `overlay-shaped.body` id `flower-pot`.
 */

/** Host plugin body — the occupant is the browser half. */
export function apply(): void {}
