/**
 * @deepseek-ai/dsh-client-ui-black-hole node half. The browser half ships via exports["./client"].
 */

/** Host plugin body — the occupant is the browser half. */
export function apply(): void {}
