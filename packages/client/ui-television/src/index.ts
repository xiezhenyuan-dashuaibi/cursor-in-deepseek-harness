/**
 * `@deepseek-ai/dsh-client-ui-television` node half. The browser half ships via
 * exports["./client"] and occupies `shell.overlay` id `television`.
 */

/** Host plugin body — the occupant is the browser half. */
export function apply(): void {}
