/**
 * @deepseek-ai/dsh-client-ui-dew-journal node half. The browser half ships via exports["./client"].
 */

/** Host plugin body — the occupant is the browser half. */
export function apply(): void {}
