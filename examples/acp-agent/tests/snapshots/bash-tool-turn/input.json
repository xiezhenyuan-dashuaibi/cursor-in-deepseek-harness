{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the dsh_bash tool to run exactly: echo TERMINAL_OK. Then reply with the single word DONE and stop." }
  ]
}
