{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Delegate one question check. Ask the child to call dsh_ask_user_question once about the CUDA fallback and return any unresolved question in its final result." }
  ]
}
