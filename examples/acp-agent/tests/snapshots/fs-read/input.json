{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the dsh_read tool (NOT bash) to read the file greeting.txt in the current directory, then reply with exactly the single word DONE." }
  ]
}
