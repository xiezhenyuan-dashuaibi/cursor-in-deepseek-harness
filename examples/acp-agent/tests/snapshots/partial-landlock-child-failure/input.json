{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the dsh_bash tool to run exactly: false. Then reply with exactly CHILD_EXIT_PRESERVED and stop." }
  ]
}
