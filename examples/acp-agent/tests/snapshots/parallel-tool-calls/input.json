{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    { "op": "prompt", "text": "Use the dsh_read tool twice in the same assistant message: read a.txt and b.txt. Then reply DONE." }
  ]
}
